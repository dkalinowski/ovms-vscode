{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-ovms",
            "type": "shell",
            "command": "bazel build --copt=\"-O0\" --compilation_mode=dbg //src:ovms"
        },
        {
            "label": "run-bazel-test",
            "type": "shell",
            "command": "bazel test --test_summary=detailed --test_output=summary //src:ovms_test"
        },
        {
            "label": "build-gtest",
            "type": "shell",
            "command": "bazel build --copt=\"-O0\" --compilation_mode=dbg //src:ovms_test"
        }
    ]
}
